sudo: false
language: node_js
node_js:
  - 'stable'
  - 'lts/*'
before_script:
  - npm install -g gulp
  - npm install -g webpack
  - npm install -g electron
branches:
  only:
      - master
script:
  - npm run build
